version: 2
jobs:
  build:
    docker:
      - image: debian:sid
    steps:
      - checkout
      - run:
          name: Install
          command: 'sudo apt-get update; sudo apt-get install astyle'
      - run:
          name: Code Style
          command: ./scripts/ci/linux/codestyle.bash
