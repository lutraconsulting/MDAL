version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Install
          command: apt-get install -y astyle
      - run:
          name: Code Style
          command: ./scripts/ci/linux/codestyle.bash
