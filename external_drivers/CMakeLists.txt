SET(MDAL_EXTERNAL_DRIVER_HEADER_DIRECTORY 
  ${CMAKE_CURRENT_SOURCE_DIR}
)

IF (ENABLE_TESTS)
  ADD_SUBDIRECTORY(minimal_example)
ENDIF (ENABLE_TESTS)

IF(EXTERNAL_DRIVER_DHI_DFSU)
  ADD_SUBDIRECTORY(dhi_dfsu)
ENDIF(EXTERNAL_DRIVER_DHI_DFSU)


